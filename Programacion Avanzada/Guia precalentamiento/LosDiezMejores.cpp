/*Un amigo está creando un videojuego y le pidió que desarrolle un programa que permita manejar un 
ranking de los diez mejores jugadores.
El registo de un jugador debe almacenar el puntaje obtenido y el nombre del jugador. 
En cada momento se debe almacenar los diez mejores registros, con respecto al puntaje obtenido, 
en orden no creciente (el mejor en la posición 1, el peor en la posición 10). 
En caso de empate, el jugador primero en obtener el resultado va primero.
El programa debe leer una cantidad  de registros y, para cada uno de ellos, 
imprimir un número entero entre 1 y 10 si el resultado entró en la tabla de los diez 
mejores resultados (entre los ingresados hasta el momento), o -1 de lo contrario.
Después de procesar todos los registros, el programa debe imprimir la versión final
de la lista de los diez mejores, en el mismo formato en que los datos fueron ingresados.*/
#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
    return 0;
}