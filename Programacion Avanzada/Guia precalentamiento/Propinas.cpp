/*De acuerdo con la normativa actual, cuando un cliente es atendido en un restaurante, 
el garzón puede recibir, por concepto de propina, un 10% del valor de consumo. 
Dicha propina es voluntaria: el cliente puede desistir de su pago, pagar un porcentaje menor y, 
en casos muy excepcionales, ofrecer un porcentaje mayor. 
Cada vez que el cliente paga, se incluye el monto total de consumo más el porcentaje de propina.
Para controlar los pagos, se le ha pedido a usted que programe 
una aplicación en C++ que haga lo siguiente:
*Trabaje con arreglos consumo y propinas, ambos de tamaño 1000, ya que el garzón nunca va a atender 
a más de 1000 clientes en un mes. Ambos arreglos deben permitir 
manejar números enteros en rangos adecuados para la aplicación.
*Tenga una función inicializar_registros que permita inicializar ambos arreglos, llenándolos con el valor 0.
*Tenga una función agregar_atencion que reciba los arreglos de consumo y propinas, además de los montos consumo y propinas 
que corresponden a una atención, y registre los datos de la atención en la primera posición disponible.
*Tenga una función monto_comision que reciba los arreglos de consumo y propinas y devuelva el monto total de 
comisión que el garzón debe recibir por concepto de propinas. Recuerde que la comisión por una atención se calcula
multiplicando el valor de consumo por el porcentaje de propina (redondeando hacia abajo).
*Tenga una función main que permita al usuario trabajar de forma interactiva con las siguientes funcionalidades: 
(1) Agregar una atención. (2) Imprimir el monto total de comisión actual. (3) Terminar la sesión. 
Al inicializar una sesión con el programa, se debe reiniciar los registros usando la función inicializar_registros . 
Para los comandos (1) y (2), se debe usar las funciones agregar_atencion y monto_comision,respectivamente.*/
#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
    return 0;
}